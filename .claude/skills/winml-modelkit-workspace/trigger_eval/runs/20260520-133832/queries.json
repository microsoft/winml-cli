[
  {
    "query": "i just got a snapdragon x elite dev box and want to see how fast microsoft/resnet-50 runs on the npu. need actual latency numbers, not theoretical.",
    "should_trigger": true,
    "rationale": "Classic skill use case: HF model, Snapdragon NPU, benchmarking. Directly in scope."
  },
  {
    "query": "optimize facebook/convnext-tiny-224 for my ryzen ai laptop's npu. trying to get the lowest latency possible for a real-time demo next week.",
    "should_trigger": true,
    "rationale": "AMD NPU + HF vision model + optimization. Directly in scope."
  },
  {
    "query": "got a ViT model i need to run inference on with the intel core ultra NPU — what's the path from huggingface to actually running on the NPU?",
    "should_trigger": true,
    "rationale": "Intel NPU + ViT (vision transformer, in-scope architecture) + end-to-end deployment."
  },
  {
    "query": "i need to ship a BERT classifier to windows ai pcs. walk me through getting from the hf model to something that actually runs fast on the NPU.",
    "should_trigger": true,
    "rationale": "BERT (in scope) + Windows AI PC + NPU deployment. Core use case."
  },
  {
    "query": "winml optimize keeps erroring with some unsupported op pattern on microsoft/swin-large-patch4-window7-224 targeting QNN. inspect and export went fine. what now?",
    "should_trigger": true,
    "rationale": "Direct invocation of winml CLI + troubleshooting flow the skill covers (analyze step)."
  },
  {
    "query": "benchmark google/vit-base-patch16-224 across qnn and cpu on my snapdragon. need apples-to-apples numbers for a perf review.",
    "should_trigger": true,
    "rationale": "Snapdragon + ViT + NPU-vs-CPU comparison (a pattern in SKILL.md)."
  },
  {
    "query": "fastest way to see latency for facebook/convnext-tiny-224 on my ryzen ai npu? i don't care about saving the artifact, just need a number.",
    "should_trigger": true,
    "rationale": "Ryzen AI + quick benchmark pattern, no artifact needed."
  },
  {
    "query": "got a new hexagon npu laptop and want to try microsoft/table-transformer-detection. how do i get it running on the npu with good latency?",
    "should_trigger": true,
    "rationale": "Hexagon NPU (Qualcomm) + Table Transformer (in scope) + deployment."
  },
  {
    "query": "our segformer model needs to ship to intel npu customers. how do we prep nvidia/segformer-b1-finetuned-ade-512-512 into something that actually runs on the npu?",
    "should_trigger": true,
    "rationale": "Intel NPU + SegFormer (in scope) + production prep."
  },
  {
    "query": "before i commit to a build, is there a way to tell if microsoft/swin-large-patch4-window7-224 will actually survive the full pipeline on qualcomm npu? burned half a day last time.",
    "should_trigger": true,
    "rationale": "Pre-flight check for QNN compatibility — exactly the inspect+analyze flow the skill teaches."
  },
  {
    "query": "i've got a resnet50.onnx file already on disk from an earlier export — want to get it onto my intel core ultra NPU and see latency. don't want to re-download from huggingface.",
    "should_trigger": true,
    "rationale": "Local ONNX file input (not HF) + Intel NPU + benchmarking. Tests trigger on the non-HF branch of the BYOM pipeline."
  },
  {
    "query": "just got my snapdragon x elite dev box, python 3.11 already installed. how do i install winml? want to run microsoft/resnet-50 first to see NPU latency.",
    "should_trigger": true,
    "rationale": "Install/setup question + Snapdragon NPU + named HF model. Note Python 3.11 is wrong (skill pins 3.10), so the install section is directly relevant."
  },
  {
    "query": "need to get ultralytics/yolov8n running on my ryzen ai laptop for an object detection demo. how fast can we get it on the NPU?",
    "should_trigger": true,
    "rationale": "Object detection (in-scope architecture) + AMD NPU + latency goal. Covers object-detection which is absent from the current positive set."
  },
  {
    "query": "want a json config i can commit to git so our CI rebuilds the optimized resnet50 the exact same way every PR. target is qnn. how do i set this up?",
    "should_trigger": true,
    "rationale": "config + build for reproducibility/CI — the production path the skill recommends. Tests trigger on the config-file workflow."
  },

  {
    "query": "how do i call windows ml from a c# app? i want to load my onnx model and run inference from a WinUI 3 app.",
    "should_trigger": false,
    "rationale": "Negative — Windows ML C# SDK programming, not CLI driving. Skill is about the winml CLI, not the WinRT/SDK APIs."
  },
  {
    "query": "i want to train a bert classifier on azure ml, dataset is already prepared. how do i set up the training pipeline?",
    "should_trigger": false,
    "rationale": "Negative — training on Azure ML, not deployment via winml. Different lifecycle stage entirely."
  },
  {
    "query": "how do i get llama 3 8b running locally on my laptop? want to use it for a chatbot prototype.",
    "should_trigger": false,
    "rationale": "Negative — LLM, out of scope per skill. Should NOT trigger (user might expect it to, since 'local model' sounds similar)."
  },
  {
    "query": "i want to fine-tune microsoft/phi-3-mini-4k-instruct on my support tickets. what's the cheapest way to do that?",
    "should_trigger": false,
    "rationale": "Negative — fine-tuning (training) + LLM (out of scope). Doubly out of scope."
  },
  {
    "query": "how does qualcomm's hexagon npu compare to apple's neural engine for transformer inference? is one faster?",
    "should_trigger": false,
    "rationale": "Negative — general hardware comparison question, not actionable. No model to build."
  },
  {
    "query": "i need to generate a marketing image using stable diffusion on my rtx 4090. what's the fastest setup?",
    "should_trigger": false,
    "rationale": "Negative — Stable Diffusion (out of scope generative) + NVIDIA GPU (skill doesn't cover that path)."
  },
  {
    "query": "my Foundry Local deployment on windows server won't come up — port binding keeps failing. how do i debug?",
    "should_trigger": false,
    "rationale": "Negative — Foundry Local is a different product (model serving), not winml CLI. Adjacent name, very different scope."
  },
  {
    "query": "what's the best way to read onnx model metadata in python? need to inspect input/output shapes programmatically.",
    "should_trigger": false,
    "rationale": "Negative — general ONNX programming question, no Windows/NPU/winml angle. User wants Python API, not CLI."
  },
  {
    "query": "i need to set up onnxruntime on a mac m2 to test a vision model. which package gives me CoreML support?",
    "should_trigger": false,
    "rationale": "Negative — macOS + CoreML, winml is Windows-only. Tempting because of NPU/ONNX overlap but wrong platform."
  },
  {
    "query": "i'm building a vscode extension that bundles an onnx model and runs it locally. how should i package the model?",
    "should_trigger": false,
    "rationale": "Negative — extension packaging question, not model optimization/deployment via winml. Skill doesn't cover dev-tooling."
  },
  {
    "query": "want to call Phi Silica from my WinUI 3 app to summarize text — got any sample code? running on snapdragon x elite.",
    "should_trigger": false,
    "rationale": "Negative — Phi Silica is an OS-bundled built-in Windows model, not BYOM. Hits 'Windows + NPU + Snapdragon' keywords but it's app integration with a prebuilt model, not the winml CLI."
  },
  {
    "query": "Olive's QNN quantization keeps crashing at the calibration step — error says ORT can't load the qnn provider. how do i debug?",
    "should_trigger": false,
    "rationale": "Negative — Olive is a separate Microsoft optimization tool (not winml CLI). Mentions 'QNN quantization' which strongly overlaps; description must distinguish adjacent MS tools."
  },
  {
    "query": "how do i register a custom onnx model with windows ai foundry so copilot runtime can call it from a c# app?",
    "should_trigger": false,
    "rationale": "Negative — Windows AI Foundry / Copilot Runtime is the runtime/integration layer for OS-level AI features, not the BYOM CLI. Strong near-miss with 'custom onnx + windows'."
  },
  {
    "query": "i'm using onnxruntime-genai to run microsoft/phi-3-mini-4k-instruct on my snapdragon. want to measure tokens/sec. how do i benchmark this?",
    "should_trigger": false,
    "rationale": "Negative — ORT-GenAI is a different runtime for LLM inference, Phi-3 is out-of-scope LLM. Hits 'Snapdragon + onnxruntime + benchmark' but doubly out of scope."
  },
  {
    "query": "want to get openai/whisper-base running on my snapdragon x elite NPU for live transcription. how do i optimize it?",
    "should_trigger": false,
    "rationale": "Negative — Whisper is seq2seq audio (encoder-decoder generator), out of scope per SKILL.md. Tempting because Snapdragon + NPU + HF model all hit."
  },
  {
    "query": "trying to fine-tune bert with torch-directml backend on my arc gpu but it keeps OOMing at batch size 4. any tips?",
    "should_trigger": false,
    "rationale": "Negative — DirectML for PyTorch training, not winml inference deployment. Hits 'DirectML + bert + windows GPU' but it's training-side and framework backend, not BYOM."
  }
]
