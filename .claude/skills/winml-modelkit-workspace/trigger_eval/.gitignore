# Derived: regenerated by `python run.py --new-run` into runs/<datetime>/.
# (Old top-level location, kept here in case of legacy.)
judge_prompt.txt

# Inside each run, judge_prompt.txt is also derived.
runs/*/judge_prompt.txt
