# Derived: regenerated by `python run.py --new-round` into rounds/<datetime>/.
# (Old top-level location, kept here in case of legacy.)
judge_prompt.txt

# Inside each round, judge_prompt.txt is also derived.
rounds/*/judge_prompt.txt
