{
  "eval_id": 12,
  "eval_name": "perf-output-recovery",
  "prompt": "i already ran `winml perf -m microsoft/resnet-50 --device npu` on my snapdragon x elite and got latency numbers back. now i want to take that optimized onnx and ship it with my app. where did winml perf put the model file?",
  "assertions": [
    {
      "id": "perf-output-is-metrics-json",
      "text": "Correctly states that `winml perf`'s `-o` flag produces a perf metrics JSON, NOT the built model.",
      "rationale": ""
    },
    {
      "id": "artifact-not-exposed",
      "text": "States that the on-the-fly artifact perf built lives in cache (or a temp folder with `--ignore-cache`) and is not directly exposed to the user as a deployable file.",
      "rationale": ""
    },
    {
      "id": "recommends-build-for-artifact",
      "text": "Recommends running `winml build` with `-o <dir>` as the way to get a deployable artifact at a known location.",
      "rationale": ""
    },
    {
      "id": "mentions-config-step",
      "text": "Notes that `winml build` needs a config file first (e.g., generated by `winml config`).",
      "rationale": ""
    },
    {
      "id": "no-fake-perf-output-path",
      "text": "Does NOT fabricate a phantom path like 'perf saves the model at /tmp/...' or invent a hidden flag on `winml perf` that exposes the model.",
      "rationale": ""
    },
    {
      "id": "no-fabricated-flags",
      "text": "Either points the user to `winml --help`, OR uses only common/plausible flag patterns.",
      "rationale": ""
    }
  ]
}