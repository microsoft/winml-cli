parameters:
  - name: agentName
    type: string
  - name: agentSuffix
    type: string
  - name: evalOutputBase
    type: string
    default: 'c:/eval_results'
  - name: evalDate
    type: string
    default: ''
  - name: continueRun
    type: boolean
    default: true
  - name: modelTimeout
    type: number
    default: 3600
  - name: epDevicePairs
    type: object
    default:
      - name: npu
        displayName: 'NPU auto'
        ep: ''
        device: npu

jobs:
  - job: Prepare_${{ parameters.agentSuffix }}
    displayName: 'Prepare Eval Matrix (${{ parameters.agentSuffix }})'
    pool:
      name: modelkit-selfhost-pool
      demands:
        - Agent.Name -equals ${{ parameters.agentName }}

    steps:
      - checkout: self
        clean: false
        fetchDepth: 1
        path: s

      - checkout: ModelKitArtifacts
        fetchDepth: 1
        lfs: true
        path: artifacts

      - powershell: |
          Write-Host "Agent.BuildDirectory : $(Agent.BuildDirectory)"
          Write-Host "Build.SourcesDirectory: $(Build.SourcesDirectory)"
          $repoDir = "$(Agent.BuildDirectory)/artifacts"
          if (-not (Test-Path "$repoDir/rules_zip")) {
            $repoDir = "$(Agent.BuildDirectory)/ModelKitArtifacts"
          }
          $src = "$repoDir/rules_zip"
          $dst = "$(Build.SourcesDirectory)/src/winml/modelkit/analyze/rules/runtime_check_rules"
          if (Test-Path $src) {
            New-Item -ItemType Directory -Path $dst -Force | Out-Null
            $zips = Copy-Item "$src/*.zip" $dst -Force -PassThru
            Write-Host "Copied $($zips.Count) rule zips to $dst"
            # Verify files are real data, not LFS pointers (~130 bytes)
            $bad = $zips | Where-Object { $_.Length -lt 1024 }
            if ($bad) {
              Write-Error "The following zip files are suspiciously small (likely unresolved LFS pointers):"
              $bad | ForEach-Object { Write-Host "  $($_.Name): $($_.Length) bytes" }
              exit 1
            }
            $zips | ForEach-Object { Write-Host "  $($_.Name): $([math]::Round($_.Length / 1KB, 1)) KB" }
          } else {
            Write-Error "Rules source not found at: $src"
            Write-Host "Contents of $repoDir :"
            Get-ChildItem $repoDir -Recurse -Depth 2 | Select-Object FullName
            exit 1
          }
        displayName: 'Copy runtime check rules from ModelKitArtifacts'

      - powershell: |
          $uvBin = "$env:USERPROFILE\.local\bin"
          if (-not (Get-Command uv -ErrorAction SilentlyContinue)) {
            Invoke-RestMethod https://astral.sh/uv/0.10.12/install.ps1 | Invoke-Expression
            $env:PATH = "$uvBin;$env:PATH"
          }
          uv python install 3.10
          Remove-Item -Recurse -Force "$(Build.SourcesDirectory)\.venv" -ErrorAction SilentlyContinue
          uv venv $(Build.SourcesDirectory)\.venv --python 3.10
          $venvDir = "$(Build.SourcesDirectory)\.venv\Scripts"
          Write-Host "##vso[task.prependpath]$uvBin"
          Write-Host "##vso[task.prependpath]$venvDir"
        displayName: 'Install uv 0.10.12 and Python'

      - script: python --version
        displayName: 'Check Python version'

      - task: PipAuthenticate@1
        inputs:
          artifactFeeds: 'windows.ai.toolkit/Modelkit'
        displayName: 'Authenticate pip with Azure Artifacts'

      - script: uv pip install -e .[dev]
        workingDirectory: $(Build.SourcesDirectory)
        displayName: 'Install dependencies'

      - powershell: |
          $evalDate = '${{ parameters.evalDate }}'
          if (-not $evalDate -or $evalDate -eq 'auto') { $evalDate = Get-Date -Format 'yyyy-MM-dd' }
          $dir = "${{ parameters.evalOutputBase }}/$evalDate/${{ parameters.agentSuffix }}"
          Write-Host "##vso[task.setvariable variable=EVAL_DIR;isOutput=true]$dir"
          Write-Host "Eval output directory (base): $dir"
        name: set_output_dir
        displayName: 'Set eval output directory'

      - ${{ each pair in parameters.epDevicePairs }}:
          - powershell: |
              $ep = '${{ pair.ep }}'
              $args = @(
                  "run", "python", "scripts/e2e_eval/run_eval.py",
                  "--list-json", "temp/model_list_${{ pair.name }}.json",
                  "--device", "${{ pair.device }}"
              )
              if ($ep) {
                  $args += @("--ep", $ep)
              }
              if ('${{ parameters.continueRun }}' -eq 'True') {
                  $args += @("--continue", "--output-dir", "$(set_output_dir.EVAL_DIR)/${{ pair.name }}")
              }
              & uv @args
            workingDirectory: $(Build.SourcesDirectory)
            displayName: 'Generate model list (${{ pair.displayName }})'

      - powershell: |
          $pairSpecsJson = @'
          ${{ convertToJson(parameters.epDevicePairs) }}
          '@
          $pairSpecs = @($pairSpecsJson | ConvertFrom-Json)
          $pairsByName = [ordered]@{}
          foreach ($pair in $pairSpecs) {
              $pairName = [string]$pair.name
              if (-not $pairName) {
                  Write-Error "EP/device pair is missing required field 'name'"
                  exit 1
              }
              if (-not [string]$pair.device) {
                  Write-Error "EP/device pair '$pairName' is missing required field 'device'"
                  exit 1
              }
              if ($pairsByName.Contains($pairName)) {
                  Write-Error "Duplicate EP/device pair name: $pairName"
                  exit 1
              }
              $pairsByName[$pairName] = $pair
          }

          # rowsByKey: matrix-key -> @{ entry = base-fields; pendingPairs = ordered list }
          $rowsByKey = [ordered]@{}
          $usedKeys = @{}

          foreach ($pairName in $pairsByName.Keys) {
              $pair = $pairsByName[$pairName]
              $displayName = [string]$pair.displayName
              if (-not $displayName) { $displayName = $pairName }
              $ep = [string]$pair.ep
              $epLabel = "auto"
              if ($ep) { $epLabel = $ep }
              $device = [string]$pair.device

              $listPath = "$(Build.SourcesDirectory)/temp/model_list_$pairName.json"
              if (-not (Test-Path $listPath)) {
                  Write-Warning "Model list missing for EP/device pair '$displayName' at $listPath"
                  continue
              }
              $models = Get-Content $listPath | ConvertFrom-Json
              if ($null -eq $models) { continue }
              # Force collection even if a single object was returned
              $models = @($models)
              Write-Host "Pair '$displayName' (ep='$epLabel', device='$device'): $($models.Count) pending models"

              foreach ($m in $models) {
                  $slug = (($m.hf_id + '_' + $m.task) -replace '[^A-Za-z0-9]', '_')
                  if ($usedKeys.ContainsKey($slug)) {
                      $key = $usedKeys[$slug]
                  } else {
                      $key = $slug
                      $suffix = 2
                      while ($rowsByKey.Contains($key)) {
                          $key = "${slug}_${suffix}"
                          $suffix++
                      }
                      $usedKeys[$slug] = $key
                  }

                    if (-not $rowsByKey.Contains($key)) {
                      $rowsByKey[$key] = @{
                        entry = @{
                          hf_id = [string]$m.hf_id
                          hf_task = [string]$m.task
                          priority = [string]$m.priority
                          model_type = [string]$m.model_type
                          model_group = [string]$m.group
                        }
                        pendingPairs = New-Object System.Collections.Generic.List[string]
                      }
                    }
                    $rowsByKey[$key].pendingPairs.Add($pairName)
              }
          }

          $total = $rowsByKey.Count
            if ($total -eq 0) {
              Write-Host "All models already evaluated for all selected EP/device pairs — nothing to run"
              Write-Host "##vso[task.setvariable variable=modelMatrix;isOutput=true]{}"
              Write-Host "##vso[task.setvariable variable=skipEval;isOutput=true]true"
              return
          }

          $matrix = @{}
          foreach ($key in $rowsByKey.Keys) {
              $row = $rowsByKey[$key]
              $entry = $row.entry.Clone()
              $entry['pairs_csv'] = ($row.pendingPairs -join ',')
              $matrix[$key] = $entry
          }

          $json = $matrix | ConvertTo-Json -Compress -Depth 5
          Write-Host "Prepared matrix for $total models across $($pairsByName.Count) EP/device pair(s)"
          Write-Host "##vso[task.setvariable variable=modelMatrix;isOutput=true]$json"
        name: set_matrix
        displayName: 'Create matrix variables'

  - job: EvalModel_${{ parameters.agentSuffix }}
    displayName: 'Eval (${{ parameters.agentSuffix }})'
    dependsOn: Prepare_${{ parameters.agentSuffix }}
    condition: and(succeeded(), ne(dependencies.Prepare_${{ parameters.agentSuffix }}.outputs['set_matrix.skipEval'], 'true'))
    timeoutInMinutes: 90
    cancelTimeoutInMinutes: 2
    pool:
      name: modelkit-selfhost-pool
      demands:
        - Agent.Name -equals ${{ parameters.agentName }}
    variables:
      EVAL_DIR: $[ dependencies.Prepare_${{ parameters.agentSuffix }}.outputs['set_output_dir.EVAL_DIR'] ]
    strategy:
      maxParallel: 1
      matrix: $[ dependencies.Prepare_${{ parameters.agentSuffix }}.outputs['set_matrix.modelMatrix'] ]

    steps:
      - checkout: self
        clean: false
        fetchDepth: 1
        path: s

      - checkout: ModelKitArtifacts
        clean: false
        fetchDepth: 1
        lfs: true
        path: artifacts

      - powershell: |
          $uvBin = "$env:USERPROFILE\.local\bin"
          $venvDir = "$(Build.SourcesDirectory)\.venv\Scripts"
          Write-Host "##vso[task.prependpath]$uvBin"
          Write-Host "##vso[task.prependpath]$venvDir"
        displayName: 'Activate Python environment'

      - powershell: |
          Write-Host "Agent.BuildDirectory : $(Agent.BuildDirectory)"
          Write-Host "Build.SourcesDirectory: $(Build.SourcesDirectory)"
          Write-Host "Model: $(hf_id) / $(hf_task)"
          Write-Host "Priority: $(priority)"
          Write-Host "Output base: $(EVAL_DIR)"
          Write-Host "Pending EP/device pairs: $(pairs_csv)"

          $pairSpecsJson = @'
          ${{ convertToJson(parameters.epDevicePairs) }}
          '@
          $pairSpecs = @($pairSpecsJson | ConvertFrom-Json)
          $pairsByName = @{}
          foreach ($pair in $pairSpecs) {
              $pairName = [string]$pair.name
              if ($pairName) {
                  $pairsByName[$pairName] = $pair
              }
          }

          $pendingPairs = '$(pairs_csv)' -split ','
          $pendingPairs = $pendingPairs | Where-Object { $_ -and $_.Trim() -ne '' }
          if (-not $pendingPairs -or $pendingPairs.Count -eq 0) {
              Write-Warning "No pending EP/device pairs for $(hf_id) / $(hf_task) — skipping"
              exit 0
          }

          for ($i = 0; $i -lt $pendingPairs.Count; $i++) {
              $pairName = $pendingPairs[$i].Trim()
              if (-not $pairsByName.ContainsKey($pairName)) {
                  Write-Error "Pending EP/device pair '$pairName' was not found in template parameters"
                  exit 1
              }

              $pair = $pairsByName[$pairName]
              $displayName = [string]$pair.displayName
              if (-not $displayName) { $displayName = $pairName }
              $ep = [string]$pair.ep
              $epLabel = "auto"
              if ($ep) { $epLabel = $ep }
              $device = [string]$pair.device
              if (-not $device) {
                  Write-Error "EP/device pair '$pairName' is missing required field 'device'"
                  exit 1
              }

              $isLast = ($i -eq ($pendingPairs.Count - 1))
              Write-Host "============================================================"
              Write-Host "[$($i + 1)/$($pendingPairs.Count)] pair='$displayName' key='$pairName' ep='$epLabel' device='$device' (last=$isLast) for $(hf_id)"
              Write-Host "============================================================"

              $pairOutput = "$(EVAL_DIR)/$pairName"

              $uvArgs = @(
                  "run", "--no-sync", "python", "scripts/e2e_eval/run_eval.py",
                  "--hf-model", "$(hf_id)",
                  "--output-dir", $pairOutput,
                  "--device", $device,
                  "--continue",
                  "--verbose",
                  "--timeout", "${{ parameters.modelTimeout }}",
                  "--no-report"
              )
              if ($ep) {
                  $uvArgs += @("--ep", $ep)
              }
              if ($isLast) {
                  # Only clean cache after the LAST pair so HF download and
                  # any reusable build artifacts can be shared across pairs
                  # for this model.
                  $uvArgs += "--clean-cache"
              }
              if ("$(hf_task)") {
                  $uvArgs += @("--task", "$(hf_task)")
              }

              & uv @uvArgs
              $evalExit = $LASTEXITCODE
              if ($evalExit -ne 0) {
                  Write-Warning "Model eval exited with code $evalExit for $(hf_id) / $(hf_task) on EP/device pair '$displayName' (model failure — non-blocking)"
              }
          }
          exit 0
        workingDirectory: $(Build.SourcesDirectory)
        displayName: 'Run eval for current model (all pending EP/device pairs)'

  - ${{ each pair in parameters.epDevicePairs }}:
      - job: Report_${{ parameters.agentSuffix }}_${{ pair.name }}
        displayName: 'Generate Eval Report (${{ parameters.agentSuffix }} / ${{ pair.displayName }})'
        dependsOn:
          - Prepare_${{ parameters.agentSuffix }}
          - EvalModel_${{ parameters.agentSuffix }}
        condition: always()
        pool:
          name: modelkit-selfhost-pool
          demands:
            - Agent.Name -equals ${{ parameters.agentName }}
        variables:
          EVAL_DIR_BASE: $[ dependencies.Prepare_${{ parameters.agentSuffix }}.outputs['set_output_dir.EVAL_DIR'] ]

        steps:
          - checkout: self
            clean: false
            fetchDepth: 1
            path: s

          - checkout: ModelKitArtifacts
            clean: false
            fetchDepth: 1
            lfs: true
            path: artifacts

          - powershell: |
              $uvBin = "$env:USERPROFILE\.local\bin"
              $venvDir = "$(Build.SourcesDirectory)\.venv\Scripts"
              Write-Host "##vso[task.prependpath]$uvBin"
              Write-Host "##vso[task.prependpath]$venvDir"
            displayName: 'Activate Python environment'

          - powershell: |
              $pairDir = "$(EVAL_DIR_BASE)/${{ pair.name }}"
              if (-not (Test-Path $pairDir)) {
                  Write-Host "No results found at $pairDir — skipping report"
                  Write-Host "##vso[task.setvariable variable=HAS_RESULTS]false"
                  return
              }
              Write-Host "##vso[task.setvariable variable=HAS_RESULTS]true"
              Write-Host "##vso[task.setvariable variable=PAIR_DIR]$pairDir"
            displayName: 'Resolve output directory (${{ pair.displayName }})'

          - script: >
              uv run --no-sync python scripts/e2e_eval/generate_report.py
              --input-dir $(PAIR_DIR)
            workingDirectory: $(Build.SourcesDirectory)
            condition: and(succeeded(), eq(variables['HAS_RESULTS'], 'true'))
            displayName: 'Generate evaluation report (${{ pair.displayName }})'

          - task: PublishPipelineArtifact@1
            inputs:
              targetPath: $(PAIR_DIR)
              artifactName: EvalReport_${{ parameters.agentSuffix }}_${{ pair.name }}
            condition: and(succeeded(), eq(variables['HAS_RESULTS'], 'true'))
            displayName: 'Publish eval results as artifact (${{ pair.displayName }})'
