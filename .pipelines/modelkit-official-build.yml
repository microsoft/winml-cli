#################################################################################
#                              OneBranch Pipelines                              #
# This pipeline was created by EasyStart from a sample located at:              #
#   https://aka.ms/obpipelines/easystart/samples                                #
# Documentation:  https://aka.ms/obpipelines                                    #
# Yaml Schema:    https://aka.ms/obpipelines/yaml/schema                        #
# Retail Tasks:   https://aka.ms/obpipelines/tasks                              #
# Support:        https://aka.ms/onebranchsup                                   #
#################################################################################

trigger: none # https://aka.ms/obpipelines/triggers

parameters: # parameters are shown up in ADO UI in a build queue time
- name: 'debug'
  displayName: 'Enable debug output'
  type: boolean
  default: false

variables:
  CDP_DEFINITION_BUILD_COUNT: $[counter('', 0)] # needed for onebranch.pipeline.version task https://aka.ms/obpipelines/versioning
  DEBIAN_FRONTEND: noninteractive
  WindowsContainerImage: 'onebranch.azurecr.io/windows/ltsc2022/vse2022:latest' # https://aka.ms/obpipelines/containers

resources:
  repositories:
    - repository: templates
      type: git
      name: OneBranch.Pipelines/GovernedTemplates
      ref: refs/heads/main
    - repository: artifacts
      type: github
      name: gim-home/ModelKitArtifacts
      endpoint: github.com_yuesu_microsoft
      ref: refs/heads/main

extends:
  template: v2/OneBranch.Official.CrossPlat.yml@templates # https://aka.ms/obpipelines/templates
  parameters:
    git:
      fetchDepth: -1
    featureFlags:
      EnableCDPxPAT: false
      WindowsHostVersion:
        Version: '2022'
        Network: KS1
      networkisolation:
        policy: Permissive,CFSClean
    globalSdl:
      tsa:
        enabled: false

    stages:
      - stage: Build
        displayName: 'Build Python Package'
        dependsOn: []
        jobs:
          # ── Download Runtime Check Rules ──
          - job: DownloadRules
            displayName: 'Download Runtime Check Rules'
            pool:
              type: windows

            variables:
              ob_outputDirectory: '$(Build.SourcesDirectory)\out\rules'

            steps:
              - checkout: artifacts
                fetchDepth: 1

              - powershell: |
                  $outDir = "$(ob_outputDirectory)"
                  New-Item -ItemType Directory -Path $outDir -Force | Out-Null
                  $rulesDir = "$(Build.SourcesDirectory)\ModelKitArtifacts\op_check_results\rules"
                  $zips = Get-ChildItem "$rulesDir\*.zip"
                  Write-Host "Found $($zips.Count) rule zips"
                  $zips | Copy-Item -Destination $outDir
                  Write-Host "Copied $($zips.Count) rule zips to $outDir"
                displayName: 'Copy runtime check rules'

          # ── Build Wheel ──
          - job: BuildWheel
            displayName: 'Build'
            dependsOn: DownloadRules
            pool:
              type: windows

            variables:
              ob_outputDirectory: '$(Build.SourcesDirectory)\out'
              ob_artifactBaseName: 'drop'

            steps:
              - script: python --version
                displayName: 'Check Python version'

              - task: DownloadPipelineArtifact@2
                inputs:
                  buildType: current
                  artifactName: 'drop_Build_DownloadRules'
                  targetPath: 'src\winml\modelkit\analyze\rules\runtime_check_rules'
                displayName: 'Download runtime check rules'

              - powershell: |
                  $rulesDir = "src\winml\modelkit\analyze\rules\runtime_check_rules"
                  Write-Host "Rules directory: $rulesDir"
                  if (Test-Path $rulesDir) {
                      $files = Get-ChildItem $rulesDir
                      Write-Host "Files: $($files.Count)"
                      $files | ForEach-Object { Write-Host "  $($_.Name) ($([math]::Round($_.Length/1KB, 1)) KB)" }
                  } else {
                      Write-Host "Directory does not exist!"
                  }
                displayName: 'Debug: verify rules downloaded'

              - task: PipAuthenticate@1
                inputs:
                  artifactFeeds: 'windows.ai.toolkit/Modelkit'
                displayName: 'Authenticate pip with Azure Artifacts'

              - script: pip install build twine
                displayName: 'Install build tools'

              - script: python -m build --outdir "$(ob_outputDirectory)"
                displayName: 'Build sdist and wheel'

              - script: twine check "$(ob_outputDirectory)\*"
                continueOnError: true
                displayName: 'Validate packages for PyPI'
